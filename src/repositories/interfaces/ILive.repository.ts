import { ILiveClass } from "@/models/live-schedule.model";
import { IBaseRepository } from "@/repositories/IBase.repository";

export interface ILiveRepository extends IBaseRepository<ILiveClass> {
    
}